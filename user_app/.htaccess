# Nội dung file: C:\...\LTW_242\user_app\.htaccess

# Tùy chọn cơ bản
Options -MultiViews
# Bật công cụ Rewrite
RewriteEngine On

# Thêm RewriteBase nếu ứng dụng nằm trong thư mục con
# Đường dẫn phải khớp với đường dẫn URL bạn truy cập
RewriteBase /user_app/

# Điều kiện: Nếu đường dẫn yêu cầu không phải là một file có thật (-f)
RewriteCond %{REQUEST_FILENAME} !-f
# Điều kiện: Và cũng không phải là một thư mục có thật (-d)
RewriteCond %{REQUEST_FILENAME} !-d
# Quy tắc: Chuyển hướng tất cả các yêu cầu đó về file index.html
# [QSA] giữ lại query string, [L] là quy tắc cuối cùng
RewriteRule ^ index.html [QSA,L]